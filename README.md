## Code reproduction for an issue with Ionic's segments & refresher

This was created with the Ionic CLI using blank template. When a page reloads if it has an ion-refresher, scrolling is impaired.

To run the app:
```
npm install

ionic serve
```